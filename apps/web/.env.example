# Origen del API en build de producción (sin barra final, sin /v1).
# Ejemplo: https://api.midominio.com
# La aplicación usa siempre el prefijo /v1 sobre este origen.
# VITE_API_URL=http://localhost:3000
