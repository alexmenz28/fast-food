/** Prefijo de versión estable de la API REST (recursos versionados). */
export const API_VERSION = "v1";

/** Ruta absoluta del prefijo, p. ej. `/v1`. */
export const API_VERSION_PATH = `/${API_VERSION}`;
